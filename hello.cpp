#include <stdio.h>
#include <raylib.h>

int main(void) {
   InitWindow(300, 300, "cracked");
   Vector2 newvec;
   puts("Hello, you?");

   while(true) {
      BeginDrawing();
      ClearBackground(RAYWHITE);
      EndDrawing();
   }
   return 0;
}
